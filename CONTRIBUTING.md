# 이 저장소의 규칙

블로그 발행 저장소다. 글은 `blog-with-claude` 도구로 만들고, 결과물이 여기로 들어온다.

---

## 1. 카테고리 추가하는 법

주소는 `/posts/<카테고리>/<슬러그>/` 다. 새 카테고리는 폴더 하나로 늘린다.

```
content/posts/<영문-kebab>/_index.md
```

```yaml
---
title: "화면에 보일 이름"
description: "이 카테고리가 무엇을 다루는지 한 문장"
summary: "카테고리 카드에 뜨는 한 줄"
weight: 20      # 카테고리 페이지에서의 순서. 10, 20, 30 …
---
```

**메뉴는 손대지 않는다.** 상단 메뉴는 여섯 자리로 고정이고 `hugo.toml`의 `[menus]`에 있다.
새 카테고리는 `/categories/` 페이지가 자동으로 잡아 준다.

**카테고리 이름은 주소에 들어가므로 한 번 정하면 바꾸지 않는다.** 영문 소문자와 하이픈만.

> 카테고리는 **글이 어디에 속할지**(독자가 보는 분류)이고, 니치는 **글을 어떻게 뽑을지**
> (도구 저장소 `blog-with-claude`의 개념)다. 둘은 다른 것이며 1:1일 필요도 없다.
> 짝은 도구 저장소 `scripts/publish_to_blog.py`의 `NICHE_TO_CATEGORY`에 있다.

---

## 2. 이미 밟은 함정

| 증상 | 원인 | 해결 |
|---|---|---|
| **메뉴가 통째로 안 나옴** | `menu:`라고 씀 | 최신 Hugo는 **`menus:`** 를 쓴다. `menu:`는 조용히 무시된다. 지금 메뉴는 `hugo.toml`에 있다 |
| 글이 사이트에 안 보임 | `draft: true` | 버그가 아니라 일부러 깐 안전장치다. 아래 3번 참조 |
| 글이 안 보이는데 draft도 아님 | `date`가 오늘보다 미래 | Hugo가 예약 글로 숨긴다. 매일 07:17(KST) 빌드 때 자동으로 뜬다 |
| 디자인이 깨져 글자만 휑함 | `baseURL` 불일치 | `hugo.toml`의 baseURL이 실제 주소와 정확히 같아야 한다 |
| 빌드는 됐는데 사이트가 옛날 화면 | 브라우저 캐시 | 강력 새로고침 또는 시크릿 창 |
| **카카오톡에 공유하면 그림이 안 뜸** | `cover`에 `relative: true`가 빠짐 | 없으면 og:image가 사이트 루트를 가리켜 404가 된다. 아래 2-1 참조 |
| 목록에 `📌 2026년 8월 10일 기준…`이 뜸 | front matter에 `summary`가 없음 | 없으면 Hugo가 본문 앞부분을 잘라 쓴다 |

점검 순서는 **① Actions 빌드 로그 → ② baseURL → ③ 캐시**.

---

## 2-1. 글마다 커버 그림이 있어야 한다

목록에서는 그림이 먼저 눈에 들어오고 제목은 그다음이다. 커버가 없는 글은 그 줄만 휑하게 비어 눈에 띄지 않는다. 그래서 **모든 글에 커버를 넣는다.**

```yaml
summary: "소득 기준이 중위 100%로 넓어졌어요. 월 20만원, 신청은 출생 60일 안에."
cover:
  image: "<슬러그>-thumbnail.png"
  relative: true
  alt: "그림이 무엇을 보여주는지 서술"
  caption: "캡션만 읽어도 핵심이 남는 문장"
```

| 키 | 없으면 |
|---|---|
| `image` | 목록에서 이 글만 그림 없이 제목만 뜬다 |
| **`relative: true`** | **카카오톡·페이스북 공유 그림이 깨진다.** og:image가 사이트 루트를 가리키기 때문이다 |
| `alt` | 화면 낭독기가 그림을 건너뛴다 |
| `summary` | 목록 요약 자리에 본문 첫 줄(`📌 …기준`)이 뜬다 |

**커버 그림을 본문 맨 위에 또 넣지 않는다.** 커버는 제목 아래에 자동으로 나오므로, 본문에도 두면 같은 그림이 두 번 나온다.

`layouts/_partials/cover.html`이 테마 기본 partial을 덮어쓰고 있다. 화면에 보이는 커버는 webp로 줄여 내보내고, 공유용 og:image만 원본 png를 쓴다 — 크롤러는 webp를 확실히 읽지 못하는 곳이 있다.

---

## 2-2. 본문에서 쓸 수 있는 강조 상자

마크다운 알림 문법이 다섯 갈래 상자로 나온다.

```markdown
> [!WARNING]
> 총액이 아니라 건강보험료 항목만 보셔야 합니다.

> [!TIP] 잘 안 알려진 규정이에요
> 유형 뒤에 적으면 제목이 바뀝니다.
```

| 표기 | 기본 제목 | 무엇을 담나 |
|---|---|---|
| `[!SUMMARY]` | 요약 | 글 첫머리 결론. 안 읽고 나가도 남아야 하는 것 |
| `[!WARNING]` | 주의 | 놓치면 독자가 손해를 보는 것 |
| `[!TIP]` | 참고 | 알아두면 이득이지만 몰라도 손해는 아닌 것 |
| `[!ACTION]` | 할 일 | 글을 덮고 나서 할 행동 |
| `[!NOTE]` | 알아두기 | 부연·예외 |

**글 하나에 3~6개.** 더 많으면 본문이 상자 사이 여백처럼 보인다.
모르는 유형을 쓰면 회색 기본 상자로 나간다 — 오타 하나로 문단이 사라지지 않게 한 것이다.

`> [!IMPORTANT]`와 `> [!CAUTION]`도 받는다(각각 할 일·주의로). 밖에서 글을 가져와도 안 깨진다.

**HTML을 본문에 박지 않는다.** 같은 원고가 네이버로도 나가는데 거기서는 태그가 글자로 보인다.

스타일 점검용 글이 `content/_styleguide/`에 있다. `draft`라 발행되지 않는다.

```bash
hugo server -D
```

---

## 3. `draft: true`가 기본이다

자동으로 만들어진 글은 **예외 없이 `draft: true`로 들어온다.** 사람이 읽고 손으로 `false`로 바꾸기 전에는 발행되지 않는다.

이것이 자동 발행과 자동 정책 위반을 가르는 빗장이다. 편하자고 기본값을 뒤집지 않는다.

대기 중인 글 보기:

```bash
hugo list drafts
```

---

## 4. 슬러그(주소)는 한 번 정하면 못 바꾼다

- 영문 소문자 + 하이픈 (`first-meeting-voucher-2026`)
- **발행한 뒤에는 절대 바꾸지 않는다.** 주소가 바뀌면 검색 순위와 외부 링크가 끊긴다.
- 같은 슬러그가 이미 있으면 **덮어쓰지 말고 건너뛴 뒤 보고**한다.

---

## 5. 면책 문구는 건드리지 않는다

`layouts/_partials/extend_post_content.html`이 `content/posts/` 아래 모든 글에 면책 문구를 자동으로 붙인다. **개별 글의 front matter로는 끌 수 없다. 그게 의도다.**

사람의 기억에 의존하는 규칙은 언젠가 깨지지만, 템플릿에 박아 둔 규칙은 깨지지 않는다.

- 기준일은 `lastmod`(없으면 `date`)에서 자동으로 뽑는다.
- 카테고리마다 문구가 다르고, 새 카테고리는 공통 문구를 받는다.
- 제휴 링크가 있는 글은 front matter에 `affiliate: true`를 넣는다. 고지 문단이 추가된다.

---

## 5-1. 본문 글꼴은 '수정본'이라 지킬 게 있다

본문 글꼴은 네이버 **마루 부리**를 웹용으로 변환·서브셋한 것이다. SIL Open Font License 1.1을 따르는데, 포맷을 바꾸거나 글자를 덜어내면 라이선스가 말하는 **수정본(Modified Version)** 이 된다. 원본을 그대로 쓰는 것과 조건이 다르다.

| 조항 | 우리가 한 것 | 파일 |
|---|---|---|
| §3 예약 글꼴 이름 금지 | 내부 이름을 `Onioni Serif`로 바꿈 | 글꼴 파일 안 name 테이블 |
| §2 저작권 안내·전문 동봉 | 전문 배치 + 글꼴 안에 메타데이터 삽입 | `static/fonts/OFL.txt` |
| 출처 표기 (네이버 권장) | 푸터에 한 줄 | `layouts/_partials/extend_footer.html` |

**`MaruBuri`라는 이름을 파일명·CSS·글꼴 내부 어디에도 되돌려 쓰지 않는다.** 예약된 이름이라 수정본에는 못 쓴다.

굵기를 늘리거나 서브셋을 손볼 때는 손으로 만들지 말고 스크립트를 쓴다. 이름 변경과 라이선스 메타데이터 삽입이 그 안에 들어 있어서, 손으로 하면 빠뜨린다.

```bash
python3 scripts/build-font.py ~/Downloads/maruburi/OTF
```

원본 OTF는 저장소에 두지 않는다. [네이버 글꼴 페이지](https://hangeul.naver.com/font)에서 받는다.

> 네이버 안내 중 알아 둘 것 — 나눔글꼴이 적용된 인쇄물·광고물의 이미지는 네이버가 글꼴 홍보에 쓸 수 있다. 원치 않으면 네이버에 요청할 수 있다.

---

## 6. 광고·분석 코드는 한곳에만

전부 `layouts/_partials/extend_head.html`에 있고, 값은 `hugo.toml`의 `[params]`에서 가져온다.

| 설정 키 | 무엇 | 언제 채우나 |
|---|---|---|
| `adsenseClient` | 애드센스 게시자 ID (`ca-pub-…`) | 승인 후 |
| `googleAnalytics` | GA4 측정 ID (`G-…`) | GA4 속성 만든 뒤 |
| `googleSiteVerification` | 서치콘솔 소유 확인 | 속성 등록 시 |
| `naverSiteVerification` | 네이버 서치어드바이저 소유 확인 | 사이트 등록 시 |

**다른 파일에 광고 코드를 적지 않는다.** 흩어 두면 스니펫 중복과 ID 오기가 난다. 값이 비어 있으면 아무것도 출력되지 않으므로, 미설정 상태로 배포해도 안전하다.

`static/ads.txt`는 **게시자 ID를 받은 뒤에 만든다.** 내용이 틀린 ads.txt는 없느니만 못하다.

---

## 7. 배포 흐름

```
main에 push  또는  매일 07:17(KST)  또는  Actions 탭에서 수동 실행
      ↓
빌드 (Hugo 0.164.0 고정)
      ↓
발행 결과 점검 — index.html · sitemap.xml · CNAME · baseURL 일치
      ↓
GitHub Pages 배포
      ↓
실패하면 → 저장소에 이슈 자동 생성 (메일 알림)
```

**빌드가 실패하면 배포 단계는 실행되지 않는다.** 새 걸 못 올리는 것은 불편이지만, 있던 걸 깨뜨리는 것은 사고다. 실패해도 마지막 정상 사이트가 그대로 남는다.

### Hugo 버전을 `latest`로 두지 않는 이유

외부 부품은 우리 허락 없이 버전을 올린다. 잠든 사이 공구가 새 모델로 바뀌어 아침에 손에 안 맞는 상황을 막으려고 `.github/workflows/publish.yml`의 `HUGO_VERSION`에 번호를 박아 두었다. **로컬 개발 환경과 같은 번호를 유지한다.** 월 1회 점검 때 올릴지 확인한다.

**지금 실제로 그럴 위험이 있다.** 빌드할 때마다 아래 경고가 뜬다.

```
.Language.LanguageDirection was deprecated in Hugo v0.158.0 and will be removed
.Language.LanguageCode was deprecated in Hugo v0.158.0 and will be removed
```

우리 설정이 아니라 **PaperMod 테마 안**에서 나는 경고다. 우리가 고칠 수 없다.
Hugo가 이 기능을 실제로 제거하면 테마가 깨지고 빌드가 실패한다.

- 지금은 Hugo 0.164.0에 고정돼 있어 안전하다
- **Hugo 버전을 올리기 전에 반드시 테마도 함께 최신으로 올리고 로컬에서 빌드해 본다**
- 순서를 지킨다: 테마 업데이트 → 로컬 빌드 성공 확인 → 그다음 `HUGO_VERSION` 상향

```bash
git submodule update --remote themes/PaperMod && hugo --buildDrafts
```

---

## 8. 월 1회 점검

| # | 항목 | 확인 |
|---|---|---|
| 1 | 발행 기록 | Actions 탭에 빨간 X가 며칠 있었나 |
| 2 | 키워드 큐 잔량 | 한 달 치 이하면 보충 |
| 3 | 중복·품질 | 최근 글 2~3편 직접 읽기 |
| 4 | 부품 최신성 | Hugo·액션 버전, 보안 업데이트 |
| 5 | 시크릿·계정 | API 키 한도, 애드센스 정책 센터 경고 |

정지는 벼락이 아니다. 대개 그 전에 노란불이 먼저 켜진다. 문제는 노란불을 못 보고 지나치는 것이다.
